import type { CharacterSummary } from "netim2-shared"

export const CharacterSelectionCard = ({ character }: { character: CharacterSummary }) => {
    return (
        <section className="text-orange-200 mb-[3rem]  p-2 bg-[url('/characterSelection/bg-pj_2.png')] bg-repeat bg-center border border-black w-[500px]">
            
        </section>
    )
}